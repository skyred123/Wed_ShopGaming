@model IEnumerable<Wed_ShopGaming.ViewModels.HinhAnhMainViewModel>
@{
    ViewBag.Title = "CheckOut";
    Wed_ShopGaming.Models.ApplicationUser user = ViewBag.User;
    Session["ThanhToan"] = Model.ToList();
}


<!-- Checkout Start -->
@using (Html.BeginForm("ThanhToan", "ThanhToan", FormMethod.Post)) { 
    <div class="container-fluid">
        <div class="row px-xl-5">
            <div class="col-lg-7">
                <h5 class="section-title position-relative text-uppercase mb-3"><span class="bg-secondary pr-3">Thong Tin Giao Hang</span></h5>
                <div class="bg-light p-30 mb-5">
                    <div class="row">
                        <div class="col-md-12 form-group">
                            <label>Ten</label>
                            <label class="form-control disabled" type="text">@user.UserName</label>
                        </div>
                        <div class="col-md-12 form-group">
                            <label>E-mail</label>
                            <label class="form-control" type="text" placeholder="">@user.Email</label>
                        </div>
                        <div class="col-md-12 form-group">
                            <label>So Dien Thoai</label>
                            <label class="form-control" type="text" placeholder="">@user.PhoneNumber</label>
                        </div>
                        <div class="col-md-12 form-group">
                            <label>Dia Chi</label>
                            <label class="form-control" type="text" placeholder="">@user.Email</label>
                        </div>
                    </div>
                </div>
                <div class="mb-5">
                    <h5 class="section-title position-relative text-uppercase mb-3"><span class="bg-secondary pr-3">Hinh Thuc Thanh Toan</span></h5>
                    <div class="bg-light p-30">
                        <div class="form-group">
                            <div class="custom-control custom-radio">
                                <input type="radio" class="custom-control-input" name="payment" value="paypal" id="paypal">
                                <label class="custom-control-label" for="paypal">Paypal</label>
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="custom-control custom-radio">
                                <input type="radio" class="custom-control-input" name="payment" value="COD" id="directcheck">
                                <label class="custom-control-label" for="directcheck">Thanh Toan Khi Nhan Hang</label>
                            </div>
                        </div>
                        <div class="form-group mb-4">
                            <div class="custom-control custom-radio">
                                <input type="radio" class="custom-control-input" name="payment" value="MOMO" id="banktransfer">
                                <label class="custom-control-label" for="banktransfer">MOMO</label>
                            </div>
                        </div>
                        <input type="submit" class="btn btn-block btn-primary font-weight-bold py-3" value="Thanh Toan"/>
                    </div>
                </div>
            </div>
            <div class="col-lg-5">
                <h5 class="section-title position-relative text-uppercase mb-3"><span class="bg-secondary pr-3">Tổng số đơn đặt hàng</span></h5>
                <div class="bg-light row  mb-5">
                    <div class="col-lg-12 border-bottom">
                        <h6 class="">Products</h6>
                        <table>
                            @foreach (var item in Model)
                            {
                                <tr class="n">
                                    <th class="col-lg-3">
                                        
                                        <p>@item.sanPham.Name</p>
                                    </th>
                                    <th class="col-lg-3">
                                        @Html.Label(item.sanPham.Price.ToString())
                                    </th>
                                    <th class="col-lg-3">
                                        @Html.Label(item.count.ToString())
                                    </th>
                                    <th class="col-lg-3">
                                        @Html.Label(@String.Format("{0:0,0}", item.sanPham.Price * item.count))
                                    </th>
                                </tr>
                            }
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
<!-- Checkout End -->
